{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "add-deps": "lerna bootstrap",
    "del-deps": "lerna bootstrap",
    "version": "lerna version patch --no-push  --conventional-commits",
    "publish": "lerna publish from-git",
    "diff": "lerna diff",
    "build": "lerna run build",
    "test": "lerna run test"
  }
}
