{
  "name": "rootpck",
  "version": "0.1.2",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "add-deps": "lerna bootstrap",
    "del-deps": "lerna bootstrap",
    "publish": "lerna publish from-git",
    "diff": "lerna diff",
    "build": "lerna run build",
    "test": "lerna run test"
  }
}
